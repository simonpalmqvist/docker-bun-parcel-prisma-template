{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "db:migrate": "prisma migrate dev --name init",
    "db:studio": "prisma studio",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run db:migrate && bun run --watch src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^5.14.0"
  },
  "module": "src/index.js"
}
